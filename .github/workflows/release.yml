name: "Release"
on:
  repository_dispatch:
    types: release

jobs:

  release:
    name: "Release"
    runs-on: ubuntu-latest
    steps:
      - name: "Verify repository dispatch"
        run: |
          echo "SUCCESS"

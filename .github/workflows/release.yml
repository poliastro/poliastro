name: "Release"
on:
  workflow_dispatch:

jobs:

  release:
    name: "Release"
    runs-on: ubuntu-latest
    steps:
      - name: "Verify repository dispatch"
        run: |
          echo "SUCCESS"
